H) Integrations (NEW MODULE)
I1 List (table + empty/error/loading)
   Columns: Name • Public ID • Envs (badges: staging/prod) • Status (Active/Paused/Archived) • Last Publish • Queries (7d) • Errors (7d) • Owner
   Row actions: Open • Duplicate • Pause/Resume • Archive
   Bulk actions: Pause/Resume/Archive
   Filters: Status, Owner, Env, Date; Search by name/public ID
   Empty CTA: “Create Integration”
I2 Create/Edit (tabs with live preview on right)
   Overview: Name, Slug(auto), Description, Status, Owner(people picker), Tags
   Embed & Keys: “Create Key” (show-once modal: label, env, rate limit, expiry) • Keys table (Label, Prefix, Env, Rate Limit, Created, Last Used, Actions: Rotate/Revoke) • Embed snippets (script/React/WebView) • Public ID (Regenerate)
   Domains: allowlist table (Origin, CSP helper, Added on, Remove) • “Add Domain” modal with examples • Warning if empty
   Config (RAG & Behavior): Mode (Search/Chat/Auto); Behavior (showLauncher, defaultOpen, panelWidth, mobilePresentation bottomSheet/fullScreen); RAG (topK, similarityThreshold, reranker, maxAnswerLen, temperature, requireCitations, language auto/en/…); Privacy (redactPII, sessionTTL, storeTranscripts, logSampling); Callbacks (onReady/onAnswer/onFeedback) • JSON view toggle + validation + “Restore default” • Save creates DRAFT (banner)
   Theme: color tokens, radius, font choice, “use host fonts” toggle; light/dark preview; import/export tokens (JSON)
   Environments: cards for Staging/Production (+ custom); Base URL, Default flag, Notes; “Promote to production” pattern
   Webhooks & Event Log: Webhooks table (URL, Env, Secret(copy), Events, Active, Last delivery, Actions); “Test webhook” → delivery result panel; Event Log filter by type (answer.created, answer.completed, search.performed, feedback.created, error) with payload preview
   Versions (History & Diff): list v1…vn (Created by, Date, Notes, Status Live/Draft); Compare diff (side-by-side JSON); Actions: Publish, Rollback, Duplicate to Draft
   Analytics (per-integration): KPIs (queries 7/30d, p95, satisfaction, errors); charts; Top queries; Top sources; Errors table → sessions
   A/B & Canary (optional): variants A/B/C with weight sliders mapped to Config Versions; Canary % traffic; “Promote”; warning banners
   Permissions (optional): share with members/teams (viewer/config/admin) • per-integration overrides of org defaults
   Audit (optional): change log (key create/rotate/revoke, domain add/remove, config publish/rollback)