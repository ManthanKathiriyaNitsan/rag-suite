[build]
  command = "npm run build:client"
  publish = "dist/public"

# Single Page App (React + Vite) redirect so client routing works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


