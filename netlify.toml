[build]
  command = "npm run build:netlify"
  publish = "dist/public"

# Single Page App (React + Vite) redirect so client routing works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


